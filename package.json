{
  "name": "1min_meditation",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "cp index.source.html index.html",
    "prebuild": "cp index.source.html index.html && node scripts/generate-icons.mjs",
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node scripts/copy-dist-to-root.mjs",
    "preview": "vite preview",
    "lint": "eslint src --ext .js,.jsx"
  },
  "dependencies": {
    "preact": "^10.26.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "eslint": "^8.57.0",
    "sharp": "^0.34.2",
    "vite": "^6.3.5",
    "vite-plugin-pwa": "^1.0.0"
  }
}
